# ─── Supabase ─────────────────────────────────────────────────────────────────
# Get these from: https://supabase.com/dashboard → your project → Settings → API
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJ...

# ─── Anthropic ────────────────────────────────────────────────────────────────
# Get from: https://console.anthropic.com/settings/keys
# Never expose this in client-side code — server-only
ANTHROPIC_API_KEY=sk-ant-...

# ─── Sentry (optional — error monitoring) ─────────────────────────────────────
# Get from: https://sentry.io → your project → Settings → Client Keys (DSN)
# Leave unset in local dev to disable Sentry silently
NEXT_PUBLIC_SENTRY_DSN=https://...@oXXXXXXXX.ingest.sentry.io/XXXXXXX

# Required only for uploading source maps during CI builds (optional)
SENTRY_ORG=your-sentry-org-slug
SENTRY_PROJECT=ai-navigator
SENTRY_AUTH_TOKEN=sntrys_...

# ─── PostHog (optional — product analytics) ───────────────────────────────────
# Get from: https://us.posthog.com → your project → Settings → Project API key
# Leave unset in local dev to disable PostHog silently
NEXT_PUBLIC_POSTHOG_KEY=phc_...
# Default host works for US cloud; EU cloud: https://eu.i.posthog.com
NEXT_PUBLIC_POSTHOG_HOST=https://us.i.posthog.com
